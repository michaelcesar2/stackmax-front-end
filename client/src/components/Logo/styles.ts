'use client';

import styled from 'styled-components';

export const Wrapper = styled.div`
  transition: 1s;
  display: flex;
  color: #f8f8f8;

  &:hover {
    transform: scale(1.05);
    cursor: pointer;
  }
`;
